Charcoal iPhone bezel with a notch — the right-canvas mobile preview shell. Children fill the screen.

```jsx
<PhoneFrame>
  <div style={{ paddingTop: 32, flex: 1, overflowY: "auto" }}>
    {/* 小红书 note detail / feed */}
  </div>
</PhoneFrame>
```

- Fixed 9:18.5 aspect; set `width` to scale.
- Pad screen content by ~32px at the top to clear the notch.
